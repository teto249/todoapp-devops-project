pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Performance Test (Multiple Runs)') {
            steps {
                echo 'Running performance test